name: Update RSS news

on:
  schedule:
    # 06:17 Asia/Tokyo is 21:17 UTC on the previous day.
    - cron: "17 21 * * *"
    # 18:23 Asia/Tokyo is 09:23 UTC on the same day.
    - cron: "23 9 * * *"
  workflow_dispatch:
    inputs:
      use_existing_news:
        description: "Skip RSS/Gemini fetch and deploy the repository news.json"
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch RSS feeds
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.use_existing_news != true }}
        run: python scripts/fetch_feeds.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TRANSLATION_LIMIT_PER_CATEGORY: "10"
          TRANSLATION_DELAY_SECONDS: "70"

      - name: Refresh existing news timestamp
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.use_existing_news == true }}
        run: |
          python - <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          path = Path("news.json")
          data = json.loads(path.read_text(encoding="utf-8-sig"))
          now = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
          data["generatedAt"] = now
          data["generated_at"] = now
          path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
          print(f"Refreshed news.json timestamp to {now}")
          PY

      - name: Run tests
        run: python -m unittest discover -s tests

      - name: Commit generated news.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add news.json
          if git diff --cached --quiet; then
            echo "news.json has no changes to commit."
          else
            git commit -m "Update generated news.json"
            git push
          fi

      - name: Build public site
        run: python scripts/build_site.py

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
